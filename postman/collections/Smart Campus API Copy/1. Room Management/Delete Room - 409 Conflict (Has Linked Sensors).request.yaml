$kind: http-request
description: Attempts to delete room with ID 1, which has linked sensors. Expects HTTP 409 Conflict.
url: "{{baseUrl}}/rooms/1"
method: DELETE
headers:
  Accept: application/json
order: 4000
