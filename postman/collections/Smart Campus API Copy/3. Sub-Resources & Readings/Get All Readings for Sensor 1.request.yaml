$kind: http-request
description: Retrieves all readings associated with sensor ID 1. Expects HTTP 200 OK with a list of readings.
url: "{{baseUrl}}/sensors/1/readings"
method: GET
headers:
  Accept: application/json
order: 2000
